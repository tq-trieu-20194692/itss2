import {UrlQuery} from "../../../core/UrlQuery";
import {_T_Params, T_QueryVO} from "../../../models/UserModel";
import {useNavigate} from "react-router";
const OneDiaryScreen = () => {
    const navigate = useNavigate()
    return (
        <>

        </>
    );
};

export default OneDiaryScreen;
